mdns:
  services:
    - service: "_zigbee-coordinator"
      protocol: "_tcp"
      port: 6638
      txt:
        version: 1
        name: ESPHome ${ name }
        radio_type: ${ type }
        serial_number: !lambda "return esphome::get_mac_address();"
